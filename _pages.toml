# Cloudflare Pages configuration for Gramátike
# Note: Main deployment uses Cloudflare Workers via wrangler.toml
# This file is only used if deploying via Cloudflare Pages

[build]
  command = "python -m pip install -r requirements-prod.txt"
  publish = "gramatike_app/static"

[build.environment]
  PYTHON_VERSION = "3.12"

# Note: Routes are handled by Cloudflare Workers (index.py), not Pages Functions
# The [[redirects]] section was removed as it conflicted with Workers deployment
