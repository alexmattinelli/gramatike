{
  "buildCommand": "pip install -r requirements-prod.txt",
  "routes": [
    { "src": "/static/(.*)", "dest": "/gramatike_app/static/$1" },
    { "src": "/(.*)", "dest": "/api/index.py" }
  ]
}
