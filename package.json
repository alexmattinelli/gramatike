{
	"name": "gramatike",
	"version": "2.0.0",
	"type": "module",
	"private": true,
	"description": "Rede social educativa de português - TypeScript Edition",
	"scripts": {
		"clean:python": "find . -name '*.py' -not -path './.git/*' -delete && find . -name 'requirements.txt' -delete && find . -name '.python-version' -delete && find . -type d -name '__pycache__' -exec rm -rf {} + || true",
		"prebuild": "npm run clean:python",
		"build": "echo 'Build complete - static site with Cloudflare Functions'",
		"dev": "wrangler pages dev public --compatibility-date=2026-01-06",
		"deploy": "wrangler pages deploy public",
		"db:migrate": "wrangler d1 execute gramatike --file=./schema.d1.sql",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@cloudflare/workers-types": "^4.20250106.0"
	},
	"devDependencies": {
		"@types/node": "^20.0.0",
		"typescript": "^5.3.0",
		"wrangler": "^4.54.0"
	},
	"engines": {
		"node": ">=20.0.0"
	}
}
